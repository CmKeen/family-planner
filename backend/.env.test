# Test Environment Variables
NODE_ENV="test"
PORT="3001"

# Database (test)
DATABASE_URL="postgresql://test:test@localhost:5432/family_planner_test?schema=public"

# JWT (test - not secure, only for tests)
JWT_SECRET="test-secret-key-for-jest-tests-only-not-for-production-use-minimum-32-chars"
JWT_EXPIRES_IN="7d"

# CORS
CORS_ORIGIN="http://localhost:5173"

# App
APP_NAME="Family Planner"
APP_URL="http://localhost:5173"

# Email / SMTP (optional for tests)
# SMTP_HOST=""
# SMTP_PORT=""
# SMTP_SECURE="false"
# SMTP_USER=""
# SMTP_PASS=""
EMAIL_FROM_NAME="Family Planner Test"
EMAIL_FROM_ADDRESS="test@familyplanner.com"
