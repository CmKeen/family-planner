# =====================================================
# Family Planner - Production Environment Variables
# =====================================================
#
# IMPORTANT: This is an EXAMPLE file. DO NOT use these values in production!
#
# Instructions:
# 1. Copy this file to .env.production
# 2. Generate strong, random values for all secrets
# 3. Never commit .env.production to version control
# 4. Use a secrets manager in production (AWS Secrets Manager, etc.)
#
# =====================================================

# =====================================================
# Database Configuration
# =====================================================

# PostgreSQL Database URL
# Format: postgresql://USERNAME:PASSWORD@HOST:PORT/DATABASE?schema=public
# Example: postgresql://myuser:strongpassword@db.example.com:5432/family_planner?schema=public
POSTGRES_USER=familyplanner
POSTGRES_PASSWORD=CHANGE_THIS_TO_STRONG_RANDOM_PASSWORD
POSTGRES_DB=family_planner
DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}?schema=public

# =====================================================
# JWT Authentication
# =====================================================

# JWT Secret Key - MUST be changed in production!
# Generate with: openssl rand -base64 64
# Minimum 32 characters, recommended 64+
JWT_SECRET=CHANGE_THIS_TO_A_VERY_LONG_RANDOM_SECRET_AT_LEAST_64_CHARACTERS_LONG
JWT_EXPIRES_IN=7d

# =====================================================
# Backend API Configuration
# =====================================================

# Server Port
PORT=3001

# Node Environment
NODE_ENV=production

# CORS Origin - Your frontend URL(s)
# For multiple origins, separate with commas
# Example: https://app.example.com,https://www.example.com
CORS_ORIGIN=https://your-frontend-domain.com

# App Information
APP_NAME=Family Planner
APP_URL=https://your-domain.com

# =====================================================
# Frontend Configuration
# =====================================================

# Backend API URL
VITE_API_URL=https://your-backend-domain.com/api

# =====================================================
# Security Notes
# =====================================================
#
# 1. JWT_SECRET:
#    - Must be at least 32 characters
#    - Use random, cryptographically secure string
#    - Never share or commit to version control
#    - Rotate periodically (will invalidate all tokens)
#
# 2. Database Password:
#    - Use strong, random password (20+ characters)
#    - Mix uppercase, lowercase, numbers, symbols
#    - Different from other passwords
#    - Store in secrets manager in production
#
# 3. CORS_ORIGIN:
#    - Set to your actual frontend domain
#    - Use HTTPS in production
#    - Be specific (don't use wildcards)
#
# 4. Database URL:
#    - Use managed database service (not localhost)
#    - Enable SSL/TLS connections
#    - Use connection pooling
#    - Backup regularly
#
# =====================================================
# Generating Secrets
# =====================================================
#
# Generate random JWT secret:
#   openssl rand -base64 64
#
# Generate random password:
#   openssl rand -base64 32
#
# Generate UUID:
#   uuidgen
#
# =====================================================
